/* --------------------------------------------------------------------------
   AwardModel
   -------------------------------------------------------------------------- */

window.Award = Backbone.Model.extend({

	urlRoot: '/api/awards'

});

/* --------------------------------------------------------------------------
   AwardCollection
   -------------------------------------------------------------------------- */

window.AwardCollection = Backbone.Collection.extend({

	model: Award,

	url: '/api/awards'

});
